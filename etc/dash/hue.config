# Replace with the IP address of your Hue bridge
# alternately execute hue.sh autoconfig

HUE_SEARCH_STRING="hue personal wireless lighting"
HUE_BUTTON_SEARCH_STRING="link button not pressed"

BRIDGE=
# get Username (http://www.developers.meethue.com/documentation/getting-started)
# press button and then execute command
# curl -s -X POST -d '{"devicetype":"hue_script"}'  http://$BRIDGE/api/ '.[0] | jq -r .error.description + .success.username'
USER=
